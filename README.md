Do NOT need to initialse project as package.json already created 

Required dependencies 
npm install express sequelize sqlite3 dotenv joi cors

To run npm start

CLIENT: 
npm install aframe
npm install reactrouter.dom - change to actual installation i dont remember
npm run dev

SERVER:
node server.js
Successful: shows running on port 3000
Can access and see server info such as /api/tools on localhost:3000
